#include <stdio.h>
int main()
{
    // int a=6;
    // int b=5;
    // int sum=a+b;
    // printf("%d + %d = %d",a,b,sum);  //%d is for integer

    int number;
    printf("Enter your age:", number);
    scanf("%d", &number);
    printf("age:%d", number);

    return 0;
}
